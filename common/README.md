# common

Optional shared code and data for experiments.

- `assets/` — models, textures, static files
- `lib/` — small utilities reused across experiments

Experiments should remain runnable on their own; treat this as opt-in shared material, not a required framework layer.
